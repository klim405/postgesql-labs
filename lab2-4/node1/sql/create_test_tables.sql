CREATE TABLE weather (
    city            varchar(80),
    temp_lo         int,
    temp_hi         int
);

CREATE TABLE car (
    number       int,
    color        varchar(80),
    capacity     int
);

CREATE TABLE house (
    room_count   int,
    country      varchar(80)
);